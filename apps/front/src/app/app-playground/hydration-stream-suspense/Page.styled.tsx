'use client'

import styled from '@emotion/styled'
import { padding } from '@repo/utils/emotion'

export const ListUsersPageWrapper = styled('main')`
	max-width: 75rem;
	margin-inline: 'auto';
	${padding(5)}
`
